plan_id: "fix-265-atr-true-range"
plan_title: "Fix #265: Triple-barrier ATR uses True Range instead of close-close"
plan_version: "1.0.0"
author: "Hermes Agent"
created_at: "2026-07-10"

scope:
  domain: "alphaforge"
  components:
    - "alphaforge/src/alphaforge/validation/walk_forward_runner.py"
    - "alphaforge/src/alphaforge/train.py"
  issues: ["#265"]

acceptance_criteria:
  - "ATR computation uses True Range: max(high-low, |high-prev_close|, |low-prev_close|)"
  - "walk_forward_runner.py line 386: replaced np.diff(close) with vectorized TR"
  - "train.py _generate_labels_numba: replaced close-close loop with TR loop"
  - "Full local test suite: 927+ passed, 0 failed"

evidence:
  commands_run:
    - "git diff alphaforge/src/alphaforge/validation/walk_forward_runner.py"
    - "git diff alphaforge/src/alphaforge/train.py"
    - "pytest lib/tests/ integration/tests/ simulation/tests/ (927 passed, 0 failed)"

  changed_files:
    - path: "alphaforge/src/alphaforge/validation/walk_forward_runner.py"
      summary: "Replaced np.diff(close) with vectorized True Range: np.maximum(high-low, |high-prev_close|, |low-prev_close|)"
    - path: "alphaforge/src/alphaforge/train.py"
      summary: "Replaced close-close loop with True Range loop in _generate_labels_numba"
