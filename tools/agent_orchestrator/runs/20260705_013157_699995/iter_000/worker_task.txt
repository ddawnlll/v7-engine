Implement the requested task below.

## Task

Create a new alpha candidate file at alphaforge/src/alphaforge/candidates/mean_reversion_5d.py that implements a simple 5-day mean reversion factor. The factor should compute the z-score of the latest close relative to the rolling 5-day mean and standard deviation, then return -zscore (so high zscore -> sell signal). Include a function `generate_signals(data: pd.DataFrame) -> pd.Series` that takes a DataFrame with columns ['date','close'] and returns a Series of signals aligned by date. Also create a companion test script at alphaforge/src/alphaforge/candidates/test_mean_reversion_5d.py that tests the factor on synthetic data (e.g., a sine wave with noise) and prints the following report fields in its output: train_start, train_end, test_start, test_end, n_combinations, metric (use IC between signals and forward 1-day returns as the metric). In the completion summary, explicitly include these fields with their values.

## Rules

- Implement only what is requested. Do not over-engineer.
- If the task asks for code changes, run relevant tests/checks afterward.
- Do NOT modify these authority files: evaluation.py, factors.py,
  simulation_adapter.py, fast_simulator.py, authority_map.md
- Never claim success without evidence.

## Required — Completion Summary

Your final output MUST include a completion summary with these fields:

```
## Completion Summary
- Files changed: [...]
- Commands run: [...]
- train_start: <date>
- train_end: <date>
- test_start: <date>
- test_end: <date>
- n_combinations: <number of trials>
- metric: <key metric value>
- Results: [...]
- Status: SUCCESS | FAILURE | BLOCKED
```

The gate checks for these fields. If they are missing, the iteration FAILs
and your work may be discarded.
