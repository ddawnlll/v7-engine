# config/ — Root-level configuration and profile definitions.
# Config is a namespace package (PEP 420) for mode-specific simulation profiles.
# Profiles are consumed by simulation/engine/resolvers/profile_resolver.py.
