# Audit Report — Phases 0-6 Claims

| # | Claim | Finding | Verdict | Evidence |
|---|-------|---------|---------|----------|
| 1 | Winning model deployed with 105 features, from run_honest_boost.py's exact config | Model has **99 features** (not 105). Final model trained on ALL data by deploy_winning_model.py, NOT the same codepath as the WFV loop. Manifest claims "105 features" but model is 99. Metadata JSON malformed. | **NOT CONFIRMED** | `m.num_features()=99`, metadata JSON has JSON error at line 147, feature_set_id="scalp_v1_105_features" is wrong |
| 2 | Maker/taker blending gives +0.0001R delta | Correct delta is **+0.00028R** (2.8x larger). Also: 94.5% winrate uses **1:1 R:R** default profile, NOT the asymmetric profile (1.75:0.60) claimed in Phase 0. | **NOT CONFIRMED** | Cost calc shows +0.00028R; run_honest_boost.py uses `XGBoostTrainer(mode=mode)` which loads default SCALP profile (1.75:1.75) |
| 3 | Meta-labeling: 96.5%→98.9% winrate at thr=0.80 | Raw numbers correct (745→457 trades, 96.5%→98.9% WR). But trades dropped 39%. No PBO guard run. Wilson CI at N=457, 98.9%: ~[97.4%, 99.7%] — baseline 96.5% is outside CI. | **CONFIRMED with caveats** | Report in `reports/phases_1b_2_results.md`. Missing: PBO guard, Wilson CI, evaluate_meta_labeling.py results |
| 4 | "avg adjusted threshold=0.95" AND "global 0.70 works well" | **CONTRADICTORY.** Bayesian formula: adj = 0.70 + weight*(sym_wr-0.70). With weight=1.0 for 20+ trades, adj = sym_wr. Avg adj=0.95 means most symbols need higher threshold. "Global 0.70 works well" is correct for the AGGREGATE, but per-symbol optimal ranges from 0.71-1.00. | **CONTRADICTED** | `phase_4_thresholds.py`: per-symbol thresholds range 0.71-1.00. "Works well" is misleading — global 0.70 is a compromise, not optimal. |
| 5 | V7-Lite execution layer built (bar-close loop, gate_chain wiring, drift monitor, testnet) | **NONE of these exist.** Only `candidate_gate.py` manifest was created and admitted. No bar-close loop. No gate_chain.py → eligibility wiring. No drift monitor. No binance_usdm testnet verification. "Candidate admitted" ≠ "execution layer built." | **NOT CONFIRMED** | `git diff 12d1b73~1..12d1b73` shows only 2 manifest files. No code changes to eligibility.py, runtime integration, or bar-close loop. |
| 6 | ACCP report written, roadmap updated | Roadmap was updated (3 lines changed in `v7/docs/roadmap.md`). **No ACCP report exists** for this session (check `reports/accp/`). `reports/total_profitability_report.md` was NOT updated. | **NOT CONFIRMED** | `git diff bf63583~1..bf63583` shows roadmap.md changes only. No ACCP file found for this session. |
